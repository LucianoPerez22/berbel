{% extends 'global/layout_form.html.twig' %}

{% set titlePage = 'page.propiedades.title.edit' | trans %}
{% set menu = 'areas' %}
{% set pathCancel = path('propiedad_list', {'id' : entity.area.id}) %}

{% block formContent %}
    <div class="znv-half">
        {{ form_row(form.name) }}
    </div>   
    <div class="znv-half">
        {{ form_row(form.value) }}
    </div>
{% endblock %}