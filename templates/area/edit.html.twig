{% extends 'global/layout_form.html.twig' %}

{% set titlePage = 'page.areas.title.edit' | trans %}
{% set menu = 'areas' %}
{% set pathCancel = path('areas_list') %}

{% block formContent %}
    <div class="znv-half">
        {{ form_row(form.name) }}
    </div>   
    <div class="znv-half">
        {{ form_row(form.status) }}
    </div>
{% endblock %}
